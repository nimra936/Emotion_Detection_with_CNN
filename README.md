# Emotion_Detection_with_CNN
A deep learning–based facial emotion recognition system built with Convolutional Neural Networks (CNN). The project includes data preprocessing, model training, evaluation using a confusion matrix, and real-time emotion detection from images and videos using OpenCV.
